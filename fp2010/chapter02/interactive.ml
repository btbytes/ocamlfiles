(* result of sin(1.21)*2.1^8 *)

(* 1.
utop[22]> sin 1.21 *. 2.1 ** 8.0 ;;
- : float = 353.876724427
*)

(* 2.
we cannot write 23 + 11.21 because + is only defined
for the integer type and not the floating point type
*)

(* 3.
concatenate two strings.

   > "Hello" ^ " World";;
- : string = "Hello World"
*)

(* 4.
Evaluate 4 > 2 ^ true

   > 4 > 2 || true ;;
- : bool = true

*)

(* 5.
   > print_endline "hello!";;
hello!
- : unit = ()
type of the expressio is `unit`.
*)
